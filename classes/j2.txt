try to store pointers to dictionary entries. Maybe just learn the feature vectors. 


changed build_chain_dict.py defaultdict(lambda: defaultdict)
changed j and j2.py defaultdict, and the separate section with branch info. 
